export type IGHLSubaccountAuth = {
    ghl_location_id: string;
    access_token: string;  
    refresh_token: string;
    expires_in: string;
    ghl_company_id: string;
    account_type: string;
    isActive: boolean;
};
